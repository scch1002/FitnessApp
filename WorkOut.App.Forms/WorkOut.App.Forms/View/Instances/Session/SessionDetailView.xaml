<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WorkOut.App.Forms.View.SessionDetailView">
  <StackLayout>
    <Label Text="Warm Up WorkOuts" />
    <ListView ItemsSource="{Binding SessionWarmUpWorkOuts}" ItemTapped="OnItemTapped">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding WorkOutName}" Detail="{Binding WorkOutComplete}" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <Label Text="WorkOuts" />
    <ListView ItemsSource="{Binding SessionWorkOuts}" ItemTapped="OnItemTapped">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding WorkOutName}" Detail="{Binding WorkOutComplete}" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>